!       These just have to be set to maximum values.
!       The software should adjust automatically at run time if the actual
!       values are less than the maxima.

        integer MAX_REF_GATES
        integer MAX_VEL_GATES
        integer MAX_RAY_TILT

        parameter (MAX_REF_GATES = 1840)
        parameter (MAX_VEL_GATES = 1840)
        parameter (MAX_RAY_TILT = 1000)
